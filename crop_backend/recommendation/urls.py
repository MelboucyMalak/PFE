"""from django.urls import path

from recommendation import views
urlpatterns = [
    path("api/recommendation_list",views.recommendation_list_api,name="recommendation_list"),
    path("api/plan_list",views.plan_list_api,name="plan_list")
]"""
